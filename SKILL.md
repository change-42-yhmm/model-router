---
name: model-router
description: "Decompose software work into small, verifiable steps; prefer deterministic tools; route only the minimum incremental context to an authorized model; and spend deeper reasoning only where failure cost warrants it. Use for multi-step implementation, debugging, refactoring, architecture, security review, or cross-provider routing."
---

## 用户路由偏好

请求可传 `preference: auto | speed | quality`，默认 `auto`。偏好是受约束的软目标，固定优先级为：**安全/授权 > 任务能力与验收 > 用户偏好 > 成本与时延**。

- `speed` 只可为范围明确、低风险的工作降低一个候选档位。
- `quality` 只可将低风险 `fast` 工作提升为 `balanced` 的首轮候选。
- 任一偏好都不得覆盖高风险/深度推理需求、验收条件、选择证据、授权或预算。

## 运行前价格与能力检查

自动路由前刷新官方证据源并检查模型价格、能力快照。若来源不可访问、快照过期或官方页面内容发生变化但模型目录尚未人工复核，自动执行必须降级为人工批准；不得使用旧价静默继续执行。

# 模型路由器

先减少问题规模，再选择模型。这个 Skill 的目标不是尽可能多地调用高推理模型，而是在可验证的前提下，以最少的上下文、成本和等待时间完成任务。

## 运行方向

以下原则是本 Skill 的产品方向与默认执行标准，也对应 `model-router.config.json` 中的 `routingPolicy` 配置：

1. **先减少问题规模**：拆解任务，限定文件范围，提供精确上下文。通常这比更换模型更能节省成本和时间。
2. **优先确定性工具**：搜索、测试、静态分析、格式化、脚本和结构化提取必须优先作为 `tool` 步骤执行，不占用高推理模型。
3. **分离生成与验证**：用低成本模型生成候选结果，再以测试、规则或人工检查验证；只有验证失败、证据不足或达到重试上限时才升级。
4. **复用项目记忆**：维护简洁的架构摘要、接口契约和决策记录。每次任务只引用相关条目，避免反复传递长上下文。
5. **使用缓存与增量上下文**：优先按文件摘要、依赖关系和变更集取上下文，而不是整个仓库。缓存仅存放摘要和结构化元数据，不存密钥、完整提示词或源代码副本。
6. **按任务价值分配预算**：可逆、低影响的改动设定严格预算上限；把高推理预算集中到不可逆、高风险或高失败成本的节点。

## 授权边界

选择或升级模型前始终检查授权。默认 `per_step`：跨模型、深度推理或跨供应商步骤都需要批准。

- `per_step`：展示推荐、成本、最小上下文、验收方式和备选方案，等待批准。
- `budget_auto`：仅在模型白名单、单步/项目预算与有效期内自动路由；越界即停止。
- `full_auto`：仅在授权范围内自动路由。发布、删除、权限和安全变更仍必须获得显式批准。

授权只允许调用已经公开配置的 API；不要把 Codex 或 Cursor 的界面模型选择器当作可调用 API。未配置网关时，输出半自动路由表并请用户在宿主中切换。

## 工作流

1. 读取 `memory/architecture.md`、`memory/contracts.md` 与 `memory/decisions.md` 中和任务相关的条目；没有时先建立极简摘要。
2. 将任务拆为 3–10 个可独立验收的步骤。每一步必须明确文件范围、最小上下文、验证命令、风险、可逆性与副作用。
3. 把可确定性完成的工作标为 `tool`：搜索、依赖图、测试、静态检查、格式化、日志筛选、差异归纳和结构化提取。
4. 对生成性步骤先选择 `fast` 或 `balanced`。验证失败、证据不足、跨模块因果链复杂、不可逆或失败成本高时，才升级为 `deep`。
5. 上下文按“变更文件摘要 → 相邻依赖 → 接口契约 → 必要原文”逐层扩展；不要默认注入整个仓库。
6. 运行 `scripts/route-plan.mjs`（或按 `references/gateway-config.md` 配置网关），并记录匿名模型、可见用量代理、时延、验证结果、升级原因与人工覆盖原因。
7. 任务结束后仅把可复用的架构、契约或决策结论写回项目记忆。

## 路由层级

- `tool`：确定性工具与验证步骤。
- `fast`：范围清晰、低风险、可逆且容易测试的小改动或候选生成。
- `balanced`：常规实现、局部重构、普通调试与审查。
- `deep`：算法、架构权衡、跨模块故障、安全审查、迁移，或验证失败后的升级；应限定文件范围和上下文预算。

推荐输出格式：`建议 [模型/模式]：优势…；成本…；最小上下文…；验收…；不升级的风险…；备选…`。

## 可信的模型选择与切换依据

不得根据模型名称、榜单名次或单一 benchmark 自动认定“更强”。每一次模型选择、供应商切换或推理升级都必须记录可审计证据，并至少从以下六个维度判断：

1. **任务能力匹配**：用与任务相近的评测，而非总分。例如，代码与终端任务优先看 agentic coding / terminal use；长上下文任务看长上下文推理；知识型任务同时看事实可靠性和幻觉率。独立评测只能作为候选筛选，不代替本项目的验收测试。
2. **官方能力与价格**：确认所选模型支持所需的上下文长度、工具调用、结构化输出、多模态输入和地区/数据处理要求；以供应商的当前价格表计算输入、输出、缓存命中、缓存写入和推理 token 的预估成本。价格快照超过 30 天不得作为自动预算依据。
3. **时延与吞吐**：交互式任务优先比较首 token 时延；批量或长输出任务优先比较输出速度和端到端完成时间。推理模型的“思考时间”必须计入端到端时长，不能只比较输出速度。
4. **上下文与工具可行性**：模型的标称上下文窗口不是默认发送全仓库的理由。只在最小增量上下文仍不足时扩展；同时验证 JSON/结构化输出、函数调用、流式返回、速率上限和超时是否满足任务。
5. **供应商运行质量**：同一模型在不同供应商的价格、时延、吞吐、缓存策略和功能支持可能不同。选择供应商时单独比较这些维度，并使用近 7–30 天的本地遥测 P50/P95、失败率和限流率确认。
6. **项目内结果与风险**：项目自己的定向测试、人工审查、重试次数、回滚风险和失败成本优先级最高。可逆低影响任务必须有严格成本上限；不可逆、高失败成本任务才允许为更高推理档位建立预算例外。

### 证据源与职责

- **OpenAI 官方模型比较与价格**：作为 OpenAI 模型的能力、特性、上下文和计费主来源。当前资料将 Sol 定义为复杂专业工作模型、Terra 定义为智能与成本的平衡、Luna 定义为成本敏感工作负载；但这些描述不构成自动升级许可，应继续由任务和验证决定。[官方模型比较](https://developers.openai.com/api/docs/models/compare)
- **OpenRouter Models / Rankings**：用于交叉比较公开的价格、上下文、支持参数和真实使用/基准信号；排行榜是候选发现信息，不是质量保证，也不能覆盖本项目测试。[模型目录](https://openrouter.ai/models) [实时排行榜](https://openrouter.ai/rankings/)
- **Artificial Analysis**：用于独立比较能力、每任务成本、首 token 时延、端到端时延、输出速度、上下文和特定任务评测。其综合指数应按任务类型拆看，并记录查询日期和指标定义。[模型比较与方法](https://artificialanalysis.ai/models)
- **DeepSeek 官方模型、价格与 API 能力**：DeepSeek 候选必须从官方模型/价格页、思考模式和工具调用文档中确认。当前应使用 `deepseek-v4-flash` 或 `deepseek-v4-pro`，不要再把已退役的 `deepseek-chat` / `deepseek-reasoner` 当作可选模型名。两者均有思考/非思考模式：Flash 非思考适合 `fast`，Flash 思考适合经验证的 `balanced`，Pro 思考只在 `deep` 的失败证据或高失败成本成立时进入候选；最终仍以本项目定向验证、时延和完整请求成本决定。[模型与价格](https://api-docs.deepseek.com/quick_start/pricing) [思考模式](https://api-docs.deepseek.com/guides/thinking_mode) [工具调用](https://api-docs.deepseek.com/guides/tool_calls)

### 可执行的决策顺序

1. 先运行 `tool` 步骤，缩小文件范围、抽取依赖与接口，并定义验证。
2. 将候选限制为能满足必需功能、授权、数据边界、上下文和预算的模型/供应商组合。
3. 用官方价格快照估算**完整请求成本**；用独立资料做能力/时延的候选排序；再用项目近期遥测校正排序。
4. 在 `fast` 或 `balanced` 层生成候选并运行定向验证。验证通过即停止；不得因外部榜单更高而无故升级。
5. 只有出现验证失败、证据不足、跨模块因果链无法收敛，或任务被标为不可逆/高失败成本时，才升级到 `deep`。记录失败证据、预期收益、额外成本和批准状态。
6. 将实际成功率、P50/P95 时延、可见 token 代理、实际价格版本、缓存命中率、失败与升级原因回写匿名遥测；周期性淘汰在本项目中表现不佳的候选。

每个非 `tool` 步骤在计划中应包含：`selectionEvidence`（来源 URL、快照时间、指标和适用性）、`projectEvidence`（相关测试/遥测）、`costEstimate`（输入、输出、缓存、推理组成）、`latencyExpectation`、`capabilityChecks`、`riskClass` 与 `escalationTrigger`。任何字段缺失时，默认降级为“需要人工批准”，而不是自动执行。

### 宿主与自动化边界

| 场景 | 可选模型的操作方式 | 自动化边界 |
| --- | --- | --- |
| Codex、Cursor、DeepSeek 网页或桌面聊天 | Skill 输出推荐、最小上下文和验收条件；用户在宿主的模型选择器中手动切换 | 不可由本 Skill 更改正在运行的会话模型 |
| 本地 Model Router 网关 + `per_step` | 网关生成路由计划，用户批准每个模型步骤后调用 API | 半自动；每一步均保留人工闸门 |
| 本地 Model Router 网关 + `budget_auto` / `full_auto` | 仅调用已配置、已授权的供应商 API | 白名单、预算、有效期和已有能力检查通过后可自动；选择证据可逐步补齐，缺失时降级为半自动/人工批准。发布、删除、权限和安全操作仍逐步批准 |

### 可扩展的联网证据

证据不必在安装时一次性完整。`evidenceSources` 是可扩展来源清单，支持官方模型/价格/能力页、独立评测页和项目自建评测端点。运行 `node scripts/refresh-model-evidence.mjs model-router.config.json model-router.evidence.json` 会联网检查每个来源的可访问性、时间、内容类型、标题和内容哈希，生成可审计快照；它不臆造或抓取不稳定页面中的 benchmark 数值。

新供应商或用户自定义模型可新增来源条目、模型目录项与可选解析器：解析器把可信字段映射到相同的六维 schema（能力、价格、时延、上下文/工具、运行质量、项目验证）。在没有官方 API、价格或评测字段时，路由器仍可给出候选与最小上下文/验收建议，但不把该步骤升级为无人工闸门的自动执行。

## 项目记忆

`memory/` 目录包含三份短文档：

- `architecture.md`：模块职责、边界、入口、依赖关系与生成/验证命令。
- `contracts.md`：稳定接口、数据格式、错误语义与兼容性要求。
- `decisions.md`：重要决策、原因、替代方案与复查条件。

每条记录保持简短、可定位、带日期；过期结论应更新或标注失效。不得记录密钥、个人数据、完整 prompt 或大段源代码。

## 资源

- `references/gateway-config.md`：网关、策略配置与 HTTP 接口。
- `scripts/route-plan.mjs`：生成并校验路由计划。
- `scripts/telemetry-summary.mjs`：汇总匿名执行记录。
- `gateway/server.mjs`：本地 API 网关；仅在有效授权和环境变量密钥存在时执行请求。

## 仪表盘

任务完成后，运行以下命令启动本地仪表盘：

```powershell
node scripts/serve-dashboard.mjs
```

然后在浏览器打开 `http://127.0.0.1:8765`。需要使用其他端口时，将端口号作为参数传入，例如 `node scripts/serve-dashboard.mjs 9000`。

仪表盘只读取本地 `dashboard/sample-data.json`；页面图表使用 ECharts CDN。不要在仪表盘数据中记录密钥、完整 prompt、源代码或个人数据。
